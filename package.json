{
  "name": "omnifire-wrapper",
  "version": "1.1.2",
  "description": "Monorepo for Omnifire",
  "repository": {
    "url": "https://github.com/ImDanTheDev/omnifire"
  },
  "private": true,
  "scripts": {
    "build": "lerna run --scope @omnifire/* build"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
